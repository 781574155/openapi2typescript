## 发布

1. 在www.npmjs.com上生成Bypass 2FA的access token
2. 在.npmrc中添加行`//registry.npmjs.org/:_authToken=生成的token`
3. pnpm install
4. pnpm publish

## 本地测试

打包

1. pnpm install
2. pnpm run build
3. pnpm pack

在其他项目中安装（PowerShell中运行）

1. pnpm add -D C:\Users\78157\Workspaces\VsCode\openapi2typescript\easyfollow-openapi-2.0.0.tgz